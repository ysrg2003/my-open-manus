# Global LLM configuration for Gemini
[llm]
model = "gemini-2.0-flash"                                              # The LLM model to use
base_url = "https://generativelanguage.googleapis.com/v1beta/openai/"   # API endpoint URL
api_key = "GEMINI_API_KEY_PLACEHOLDER"                                  # Will be replaced by env var
temperature = 0.0                                                       # Controls randomness
max_tokens = 8096                                                       # Maximum number of tokens in the response

[llm.vision]
model = "gemini-2.0-flash"                                              # The vision model to use
base_url = "https://generativelanguage.googleapis.com/v1beta/openai/"   # API endpoint URL for vision model
api_key = "GEMINI_API_KEY_PLACEHOLDER"                                  # Will be replaced by env var
max_tokens = 8192                                                      # Maximum number of tokens in the response
temperature = 0.0                                                      # Controls randomness for vision model

# Daytona configuration - Added to fix Pydantic validation error
[daytona]
daytona_api_key = ""
daytona_server_url = "https://app.daytona.io/api"
daytona_target = "us"

# Sandbox configuration
[sandbox]
use_sandbox = false
